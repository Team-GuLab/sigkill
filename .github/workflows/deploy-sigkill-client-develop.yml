name: Deploy sigkill-client (develop)

on:
  push:
    branches:
      - main
      - develop
    paths:
      - client/**
      - .github/workflows/deploy-sigkill-client-develop.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build client
    runs-on: ubuntu-latest
    steps:
      # 1) Checkout source code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2) Set up the build environment
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('client/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      # 3) Install dependencies and build
      - name: Install dependencies
        working-directory: client
        run: pnpm install --frozen-lockfile

      - name: Build client
        working-directory: client
        env:
          VITE_WS_URL: ${{ secrets.VITE_WS_URL }}
        run: pnpm build

      # 4) Upload build artifact for deploy job
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: client-dist
          path: client/dist
          retention-days: 1

  deploy:
    name: Deploy to OCI server
    runs-on: ubuntu-latest
    needs: build
    steps:
      # 1) Download build artifact
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: client-dist
          path: dist

      # 2) Copy build files to server
      - name: Copy build to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USER }}
          key: ${{ secrets.OCI_SSH_KEY }}
          source: dist/
          target: /tmp/sigkill-client/

      # 3) Swap static files and reload nginx
      - name: Swap static files and reload nginx
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USER }}
          key: ${{ secrets.OCI_SSH_KEY }}
          script: |
            set -e

            DEPLOY_DIR=/var/www/sigkill/html
            STAGING_DIR=/tmp/sigkill-client/dist

            sudo mkdir -p "$(dirname "$DEPLOY_DIR")"
            sudo rm -rf "$DEPLOY_DIR"
            sudo mv "$STAGING_DIR" "$DEPLOY_DIR"
            rm -rf /tmp/sigkill-client

            sudo nginx -t
            sudo systemctl reload nginx
